import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import KGetCompPOS from "./components/KGetCompPOS"; // Sesuaikan path sesuai struktur folder Anda

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <KGetCompPOS />
  </React.StrictMode>
);
